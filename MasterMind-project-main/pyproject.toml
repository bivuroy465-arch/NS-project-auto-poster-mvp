[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # line length handled separately; long prompts are fine

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
